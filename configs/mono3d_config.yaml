# dataset path and Image size
dataset:
  path: "data/kitti"
  img_size: [384, 1280]

# classes
model:
  num_classes: 5

# Training Hyperparameters
train:
  epochs: 50
  batch_size: 16
  lr: 0.001
  weight_decay: 0.0001
  patience: 10
  save_dir: "weights"
